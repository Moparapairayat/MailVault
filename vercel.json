{
  "framework": "vite",
  "buildCommand": "chmod +x node_modules/.bin/tsc && npm run build",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
