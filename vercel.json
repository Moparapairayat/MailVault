{
  "framework": "vite",
  "buildCommand": "npx --no-install tsc && npx --no-install vite build",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
